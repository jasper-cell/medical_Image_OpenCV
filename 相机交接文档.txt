1、biaoding文件夹：
用于相机的标定
2、zhuo_ban_test文件夹：
尺子识别数据
3、biaoding.py:
利用opencv对相机标定
4、chizi_mobanpipei.py（优化后的尺子识别算法）:下一步是将尺子缩小处理，对尺子定位识别，然后将识别后的结果
映射到原图像，起到加速识别过程
对zhuo_ban_test文件夹中的数据进行尺子定位
5、opencv+SURF.py（优化前的尺子识别算法）:

todo:
将定位出来的尺子利用旋转矩阵将其变为水平
然后做颜色分割，得到尺子中的黑色区域（刻度值和数字）
切割出尺子的刻度值范围
识别尺子刻度之间的像素值，找出误差矩阵

最后识别目标物体的周长，面积